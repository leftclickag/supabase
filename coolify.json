{
  "name": "Supabase Core (Lean)",
  "description": "Performance-optimized Supabase with Auth, DB, Storage, Studio, Imgproxy",
  "version": "1.0.0",
  "services": {
    "supabase": {
      "env": {
        "POSTGRES_DB": {
          "default": "postgres"
        },
        "POSTGRES_HOST": {
          "default": "db"
        },
        "POSTGRES_PORT": {
          "default": "5432"
        },
        "POSTGRES_PASSWORD": {
          "generate": "password"
        },

        "JWT_SECRET": {
          "generate": "jwt"
        },
        "JWT_EXPIRY": {
          "default": "3600"
        },

        "ANON_KEY": {
          "generate": "jwt"
        },
        "SERVICE_ROLE_KEY": {
          "generate": "jwt"
        },

        "PG_META_CRYPTO_KEY": {
          "generate": "password"
        },

        "SUPABASE_PUBLIC_URL": {
          "default": "http://localhost:3000"
        },
        "SITE_URL": {
          "default": "http://localhost:3000"
        },
        "API_EXTERNAL_URL": {
          "default": "http://localhost:3000"
        },

        "STUDIO_DEFAULT_ORGANIZATION": {
          "default": "Leftclick"
        },
        "STUDIO_DEFAULT_PROJECT": {
          "default": "PROD DB"
        },

        "ENABLE_EMAIL_SIGNUP": {
          "default": "true"
        },
        "ENABLE_EMAIL_AUTOCONFIRM": {
          "default": "true"
        },

        "IMGPROXY_ENABLE_WEBP_DETECTION": {
          "default": "true"
        }
      }
    }
  }
}
